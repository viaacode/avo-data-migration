/* in source TESTBEELD */

-- Create table for mapping of 'onderwijsinstellingen' from o attr to entryUUID
CREATE TABLE reference_org_ids (
  org_id_ldap int primary key,
  org_unit_id_ldap text,
  entryuuid varchar(36)
);
